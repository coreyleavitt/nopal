## DNS manager: writes interface DNS servers to resolv.conf.

proc updateDns*(interfaceName: string, servers: seq[string]) =
  discard # not yet implemented

proc removeDns*(interfaceName: string) =
  discard # not yet implemented
